import React from "react";
import { Text } from "react-native";

export default function MainText() {
    return (
        <Text className="text-lg color-white text-center m-2">
            Bienvenido a guide UPC
        </Text>
    );
}
